<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.programmer.escrow.community.mapper.CommunityReplyMapper">

    <resultMap id="CommunityReplyResultMap" type="com.programmer.escrow.community.entity.CommunityReplyEntity">
        <id column="id" property="id"/>
        <result column="post_id" property="postId"/>
        <result column="creator_id" property="creatorId"/>
        <result column="author_name" property="authorName"/>
        <result column="content" property="content"/>
        <result column="like_count" property="likeCount"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="BaseColumns">
        id, post_id, creator_id, author_name, content, like_count, status, created_at, updated_at
    </sql>

    <insert id="insert" parameterType="com.programmer.escrow.community.entity.CommunityReplyEntity" useGeneratedKeys="true" keyProperty="id">
        insert into `community_reply` (
            post_id, creator_id, author_name, content, like_count, status, created_at, updated_at
        ) values (
            #{postId}, #{creatorId}, #{authorName}, #{content}, #{likeCount}, #{status}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <select id="selectByPostId" resultMap="CommunityReplyResultMap">
        select <include refid="BaseColumns"/>
        from `community_reply`
        where post_id = #{postId}
          and status = 1
        order by created_at asc, id asc
    </select>
</mapper>
